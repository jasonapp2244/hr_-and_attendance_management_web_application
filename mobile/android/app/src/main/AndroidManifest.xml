<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Flutter only declares INTERNET in the debug and profile manifests, for
         the tooling's own use. Without it here a RELEASE build ships with no
         network permission at all: every API call fails, and only in the build
         that reaches the store. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 13 and above will not show a notification until the user has
         granted this explicitly. Required before FCM can deliver anything. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Backup stays on, but the sign-in token is carved out of it. The two rule
         files are read by different Android versions and both are required —
         see the comments in each. The privacy policy promises this behaviour,
         so these three attributes are what keep that page honest. -->
    <application
        android:label="HR &amp; Attendance"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11 hides installed apps from each other unless they are
             declared here. Without this, canLaunchUrl reports that nothing can
             open an https link and the privacy policy and account-deletion rows
             silently do nothing on exactly the devices a reviewer is using. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
